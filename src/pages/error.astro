---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Button from '../components/Button.astro';
---

<BaseLayout title="Error - Proyecto 26" description="Something went wrong. Please try again later.">
  <Header />

  <main class="pt-[72px]">
    <section class="min-h-[calc(100vh-72px-200px)] flex items-center justify-center bg-bg-primary">
      <div class="container">
        <div class="text-center">
          <!-- Icon -->
          <div class="mb-6" data-animate="fade-up">
            <i data-lucide="triangle-alert" class="w-16 h-16 text-stars-gold mx-auto"></i>
          </div>

          <!-- Big Text -->
          <div class="mb-4" data-animate="fade-up" data-delay="100">
            <span class="text-8xl md:text-9xl font-black gradient-text">Oops!</span>
          </div>

          <!-- Message -->
          <h1 class="text-3xl md:text-4xl font-bold text-text-primary mb-4" data-animate="fade-up" data-delay="200">
            Something Went Wrong
          </h1>
          <p class="text-text-secondary text-lg mb-8 max-w-md mx-auto" data-animate="fade-up" data-delay="300">
            We're sorry, but an unexpected error occurred. Please try again later.
          </p>

          <!-- Actions -->
          <div class="flex flex-col sm:flex-row items-center justify-center gap-4" data-animate="fade-up" data-delay="400">
            <Button href="/" variant="primary">
              <i data-lucide="home" class="w-4 h-4"></i>
              Back to Home
            </Button>
            <Button href="/contact" variant="secondary">
              <i data-lucide="message-circle" class="w-4 h-4"></i>
              Report Issue
            </Button>
          </div>
        </div>
      </div>
    </section>
  </main>

  <Footer />
</BaseLayout>

<style>
  .gradient-text {
    background: linear-gradient(135deg, var(--color-accent-coral), var(--color-stars-gold));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
</style>

<script>
  import { gsap } from 'gsap';

  document.querySelectorAll('[data-animate="fade-up"]').forEach((el) => {
    const delay = parseInt(el.getAttribute('data-delay') || '0') / 1000;

    gsap.from(el, {
      y: 30,
      opacity: 0,
      duration: 0.6,
      delay,
      ease: 'power2.out',
    });
  });
</script>
